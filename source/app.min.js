// 项目文件浏览器 - 主脚本
// 版本: 2024-12-03

console.log('JavaScript 文件加载成功 - 版本 2024-12-03');

// 缓存测试函数
function testCacheUpdate() {
    console.log('缓存更新测试 - 版本 2024-12-03');
    return '缓存更新成功 - 2024-12-03';
}

// 初始化函数
function initApp() {
    console.log('应用初始化 - 版本 2024-12-03');

    // 在页面加载时显示版本信息
    if (typeof window !== 'undefined') {
        window.APP_VERSION = '2024-12-03';
        console.log('应用版本:', window.APP_VERSION);
    }
}

// 页面加载完成后执行
if (typeof document !== 'undefined') {
    document.addEventListener('DOMContentLoaded', initApp);
}

// 导出测试函数（如果需要）
if (typeof module !== 'undefined' && module.exports) {
    module.exports = { testCacheUpdate, initApp };
}
